#include "main.h"

/**
 * int _isalpha - prints and checks for alphabet characters
 */

int _isalpha(int c)
{
	if ((c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'Z'))
		return (1);
	else
		return (0);
}
